[tool.poetry]
name = "binance-spot-analytics"
version = "0.1.0"
description = "MA Crossover strategy for Binance Spot"
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
aiohttp = "^3.9.1"
python-binance = "^1.0.19"
pandas = "^2.1.4"
numpy = "^1.26.2"
pyyaml = "^6.0.1"
python-dotenv = "^1.0.0"
requests = "^2.31.0"
python-telegram-bot = "^20.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
